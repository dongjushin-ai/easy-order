import type { PaymentMethod, PaymentOutcome } from "../types/order";
interface Props { total: number; demoOutcome: PaymentOutcome; onDemoOutcome: (outcome: PaymentOutcome) => void; onSelect: (method: PaymentMethod) => void; onBack: () => void; }
export function PaymentScreen({ total, demoOutcome, onDemoOutcome, onSelect, onBack }: Props) { return <main className="screen payment-screen"><p className="eyebrow">4단계 · 결제</p><h1>어떻게 결제하시겠어요?</h1><p className="payment-total">결제할 금액 <strong>{total.toLocaleString("ko-KR")}원</strong></p><div className="payment-methods"><button onClick={() => onSelect("card")}><span aria-hidden="true">▣</span><strong>카드 결제</strong><small>카드를 꽂거나 태그해 주세요</small></button><button onClick={() => onSelect("easy-pay")}><span aria-hidden="true">◎</span><strong>간편결제</strong><small>휴대폰의 결제 화면을 준비해 주세요</small></button></div><details className="demo-controls"><summary>개발용 결제 결과 설정</summary><label>이번 결제 결과<select value={demoOutcome} onChange={(event) => onDemoOutcome(event.target.value as PaymentOutcome)}><option value="success">성공</option><option value="card-read-failed">카드 인식 실패</option><option value="approval-failed">승인 실패</option><option value="cancelled">사용자 취소</option></select></label><p>실제 고객 화면에서는 숨길 개발용 기능입니다.</p></details><nav className="screen-nav"><button onClick={onBack}>이전으로</button></nav></main>; }
