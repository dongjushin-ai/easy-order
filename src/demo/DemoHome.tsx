import {useState} from "react";
import {stores} from "../data/stores";
import {DEMO_MODE,resetDemoState} from "./demoState";
import "./demo.css";
export default function DemoHome(){const [reset,setReset]=useState(false);return <main className="demo-home"><span className="demo-badge">DEMO</span><header><p className="eyebrow">ACCESSIBLE · STORE-AGNOSTIC · HUMAN-IN-THE-LOOP</p><h1>EasyOrder</h1><p className="lead">복잡한 메뉴판 대신 몇 가지 쉬운 질문으로 원하는 메뉴를 찾는 접근성 중심 키오스크입니다.</p><div className="hero-actions"><a href="#customer">고객 키오스크 체험</a><a href="/owner/new?demo=1">점주용 매장 만들기 체험</a><a className="secondary" href="#architecture">프로젝트 구조 보기</a></div></header><section id="customer"><h2>매장을 골라 바로 체험해 보세요</h2><div className="demo-cards">{stores.map((store,index)=><article key={store.storeId}><span>{["Cafe Demo","Korean Snack Demo","Fast Food Demo"][index]}</span><h3>{store.storeName}</h3><p>메뉴 {store.menus.length}개 · 질문 {store.questions.length}개</p><div><a href={`/kiosk?store=${store.storeId}`}>고객으로 체험</a><a className="secondary" href={`/owner?store=${store.storeId}`}>점주 화면 보기</a></div></article>)}</div></section><section><h2>점주로 체험하기</h2><p>샘플 메뉴를 불러와 추천 기준 설정, 분석 데모, 직접 확인, 키오스크 미리보기까지 이어집니다.</p><a className="cta" href="/owner/new?demo=1">새 매장 만들기 시작</a><a className="text-link" href="/owner">기존 데모 매장 검토</a></section><section id="architecture"><h2>AI와 추천 엔진의 역할은 분리됩니다</h2><div className="flow"><p><strong>점주</strong><br/>메뉴 입력 → 추천 기준 → 분석 보조 → 점주 확인 → 최종 매장</p><p><strong>고객</strong><br/>최종 매장 → 동적 질문 → Soft Scoring → 추천 → 주문</p></div><ul><li>한 번의 오답으로 메뉴를 제거하지 않습니다.</li><li>카페·분식·패스트푸드에 같은 엔진을 사용합니다.</li><li>AI는 메뉴 데이터 입력을 돕고, 최종 추천은 재현 가능한 엔진이 담당합니다.</li></ul>{DEMO_MODE&&<p className="demo-ai-note">공개 데모의 “AI 분석 데모”는 사전 정의된 분석 시뮬레이션을 사용하며 실제 모델을 호출하지 않습니다.</p>}</section><footer><button onClick={()=>{resetDemoState(localStorage);setReset(true)}}>데모 초기화</button>{reset&&<output aria-live="polite">데모에서 만든 데이터만 초기화했습니다.</output>}</footer></main>}
